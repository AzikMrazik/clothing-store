meta:
  environment: node
  toolchain:
    name: npm
    version: 16

build:
  artifacts:
    - ./backend/node_modules

run:
  command: cd backend && npm run start
  containerPort: 3001
  persistenceMount: /app/uploads

env:
  NODE_ENV: production