meta:
  environment: nodejs
  toolchain:
    name: npm
    version: 18

build:
  args: --production

run:
  port: 80
  command: npm run preview
  persistenceMount: /app/data
  resources:
    limits:
      memory: 512M
    requests:
      memory: 256M
